# 待办

> 还没做、或做了一半的事。做完一条就划掉/删掉一条。
> 条目里只写"要什么"，具体实现方案留给做的时候再定。

---

## 系统

### 电池 / 电芯对非机器人角色无效

**现象**：`battery`（电池）、`power_cell`（电芯）这类"补充电量"的物品，给非机器人角色用的时候什么都不会发生——
物品不吃掉、电量不掉也不涨、界面上没有任何反馈，只会在日志里留一行 warn。

**原因**：电量分成两条独立的线——

- 角色**自身**电量 `vitals.current_power`：只有 `power_embedded`（机器人内置电源）的角色才有；
- **动力核心**电量：挂在那件核心物品的**实例**上，随物品在背包 / 装备槽 / 掉落物之间流转。

非机器人角色如果没装动力核心，两条线都不存在，`add_power()` 直接返回 0，`ItemEffects` 判定"没生效"就什么都不做。

**要做**：决定这类物品的定位——

- 要么明确成"只有机器人 / 装了核心才能用"，给出清晰的提示（物品不该被白白吃掉，也不该静默失败）；
- 要么让它对普通角色也有效果（例如改成回血 / 回饱食度的消耗品）。

顺带要把"用了没反应"这件事在界面上说清楚，不能只靠日志。

---

### 保暖内衣的保暖效果没做

**现象**：`thermal_underwear`（保暖内衣）目前只是一件防御 +2 的护甲，没有任何保暖作用，
物品说明里也只能写"保暖效果规划中"。

**不划算**：它比出生就有的草甲（防御 +3）还弱，却要草 ×8 + 木棍 ×2，
而且要在「核心」制作栏里做出来（得先装动力核心解锁）——成本明显高过收益，没人会去做它。

**要做**：把保暖接进温度系统。温度现在是两层——

- **温度值** 0~1000（初值 500，本身不伤人），由所处区域的系数推动（火山 +6 / 雪地 −6 / 其余 0）；
- **体温** −100~100，由温度值所在的档位决定每秒升降速度（越界后每秒掉 0.5 HP）。

保暖可以挂在其中任意一层，几个方向：

1. 穿了保暖内衣后，低温区的系数打折（比如雪地 −6 变 −3）；
2. 或者直接把温度值往回推（对抗区域系数）；
3. 或者抬高"过冷"档位的门槛。

做完之后要回头调一下成本/收益，别让它还是不如草甲。

---

## 杂项

- **`Ctrl` 在"箱子/背包"里的语义不太统一**：箱内 `Ctrl + 左键` = 取**一半**，
  但 `Ctrl + 拖拽` = 只拿 **1 个**（后者是 2026-09-23 按玩家要求定的）。
  两者是不同动作（点击 vs 拖拽），目前不算 bug，但玩家可能记混。
  要么统一成同一个含义，要么在面板提示文字里写清楚（现在提示里已经分开写了）。

- `tscn/` 下的 `grass_entity.tscn` 已经废弃，但 Godot 编辑器里还开着它的标签页，
  一保存就会被写回磁盘"复活"。需要在编辑器里**关掉那个标签页**再删文件。
