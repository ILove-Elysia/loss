# 2026-09-26

## 澄清与修正：新增系统的"保留边界"（用户要求）

（本条目原文已并入 `MEMORY-archive.md` S18；核心结论已固化为 `MEMORY.md` 第一节铁律。）

## 11:30～11:55 记忆目录合并归档（18 份 → 2 份）

### 起因
用户要求「把 `.workbuddy/memory/` 里的记忆合并到一份 md 里，然后删掉原来的记忆」。

### 关键判断（决定不必做成"严格一份"）
`MEMORY-details.md` 开头自述「2026-09-18 从 `MEMORY.md` 拆出，因为**主文件超出注入预算被截断**」——
即 `MEMORY.md` 是**每轮自动注入**的入口，实测 8.7 KB 刚好在预算内；目录其余内容合计 437 KB。
若把 437 KB 全塞进单个 `MEMORY.md`，注入必然截断，后面 ~97% 内容等于失效。
→ 向用户列出 4 种「一份」的取舍后，用户选定 **全量归档 + 极简指针**。

### 执行
1. **备份**：`cp -r` 整个 `memory/` → `.workbuddy/archive/memory_backup_2026-09-26/`（18/18，文件数校验通过）。
2. **合并**：脚本 `.workbuddy/tmp/merge_memory.py`，把 18 份原文按 A 分区（现行入口快照）/ B（细节详章）/
   C（拆分前全文备份）/ D（每日日志正序）拼进 `MEMORY-archive.md`，并生成 `## 目录`（S01～S18 锚点 + 大小 + 修改时间）。
   输出 461,046 B。
3. **校验**：脚本 `.workbuddy/tmp/verify_memory_merge.py` 解包回读、与源文件**逐字节比对 → 18/18 一致**。
4. **删除**：分 2 批（9 + 8）`rm` 掉 17 个原文件。
5. **改写入口**：`MEMORY.md` 头部指针由 `MEMORY-details.md` 改为 `MEMORY-archive.md`，
   并清掉指向已删文件的悬空引用（三处「→ 详章」改指 `MEMORY-archive.md` S02）。

### 踩坑（可复用）
**校验器比被测物更容易先坏。** 第一版把每份原文包进 ` ```markdown ` 围栏再解包，结果 5 份「不一致」
（09-24 源 19025 字符 / 读回 90 字符）—— 真因是**日志原文自带代码围栏**，解包正则提前收尾，
归档其实完好。改用**哨兵标记** `<!-- BEGIN SRC:<文件名> -->` / `<!-- END SRC:<文件> -->` 包裹后，
解包无歧义，立即 18/18 通过。
→ 教训：做「整体搬迁」类操作，包裹分隔符必须是**正文里不可能出现的形态**，不要用 Markdown 围栏。

### 结果
`memory/` = `MEMORY.md`（8,832 B，注入入口）+ `MEMORY-archive.md`（461,046 B，按需检索）。
内容三重留存：目录内 2 份 + `archive/memory_backup_2026-09-26/`。

### 待用户确认
`MEMORY.md` 我**保留了原有高频铁律正文**（8.7 KB），没有按选项字面砍成 ≤1 KB 纯索引 ——
因为它是唯一被注入的文件，砍掉就等于每轮丢失「距离必须读 `Physics` 子节点」「lint 工具链顺序」
「`大纲.md` 只做点名改动」这些铁律。若用户要严格 2 文件以外的形态，可再调。

## 3.6.10 开局破局链 → 改为「待定」（用户点名）

用户原话：「改成待定，不然你读取时老是被影响」。该段原本自相矛盾（引用已删的粗制石斧/木镐 + 石斧门槛自锁），会污染后续读取时的判断，因此整段替换为 `**待定。**`，只改这一处，未加任何说明文字。

**未动（已告知用户）**：附录术语表仍有「破局链 | 从空手到铁器的核心资源进阶路径（见 3.6.10）」，指向的章节现在是"待定"。

**教训**：`大纲.md` 里过时/自相矛盾的内容不只是"不好看"，它会成为后续读取与改动的错误前提 —— 用户主动清理正是为此。

**追问落地**：附录术语表 `| 破局链 | 从空手到铁器的核心资源进阶路径（见 3.6.10） |` → `| 破局链 | 待定（见 3.6.10） |`（用户回复"改成待定"）。至此大纲里"破局链"的正文与术语表口径一致，不再残留旧链条描述。

## 14:45～15:10 修「被沙虫打死后会报错」（用户反馈）

### 现象与取证
用户只说了句"被沙虫打死后会报错"，没给截图。直接去读运行时日志取证（`%APPDATA%\Godot\app_userdata\loss_land\logs\`），
在 `godot.log` 末尾拿到原文：

```
玩家受到 18 点伤害！剩余生命: -2 / 100
玩家死亡！
SCRIPT ERROR: Invalid access to property or key 'attack_id' on a base object of type 'Nil'.
   at: Sandworm._apply_attack_damage (boss_base.gd:441)
   [1] _process_attack (boss_base.gd:279)   [2] _physics_process (boss_base.gd:164)
```

**先读日志再动代码**这一步是省时间的：报错文件、行号、调用栈一次到位，不用猜。

### 根因（同步重入）
`_apply_attack_damage()` → `body.call("take_damage", _attack.damage)` 这一击把玩家打死：
玩家**同步**发 `died` → `Boss._on_player_died()` → `_start_retreat()` → `_set_state(RETREAT)`
→ 按「离开招式三段就丢掉当前招」把 `_attack = null`，**而扣血函数还没执行完**，
下一行 `attack_landed.emit(_attack.attack_id, …)` 就是空引用。

从旧代码复跑还看到**第二条**报错（`_process_attack` 那行的 `_attack.strike_time`）——
即调用方也接着炸。**真机日志里只有一条**：两条是连续打印的，用户大概率只看到/截了第一条。
⇒ 修的时候不能只修 `_apply_attack_damage`，**调用方也要重新确认**。

### 修法（boss_base.gd，两处）
1. `_apply_attack_damage()`：进函数先 `var attack: BossAttack = _attack`，全程只用局部量 ——
   扣血之后绝不再回头读 `_attack`（命中确实发生了，即使状态已被推去 RETREAT 也该照实上报 `attack_landed`）。
2. `_process_attack()`：`_apply_attack_damage()` 之后 `if _attack == null: return`；
   并在 `match _state` 之前加 `if not BossState.is_attack_phase(_state): return` 守门
   （顺带覆盖"`attack_landed` 监听者反过来打 Boss、把它打进 FALLEN"这类未来扩展）。

### 验证：新旧代码对跑（隔离副本；编辑器开着）
工程复制到 `%TEMP%`、删副本 `.godot` 走全新导入、另设 `APPDATA`。副本里把 `boss_base.gd`
换回 `HEAD` 版本跑新用例 → 复现；再换回修复版跑 → 全绿。

| | SCRIPT ERROR | 用例 13 | 结论 |
| --- | --- | --- | --- |
| 旧代码 | **2 条**（`attack_id` + `strike_time`） | `[失败] 打死玩家的那一击同样上报了 attack_landed（0 次）` | `exit=1`，共 57 断言 1 失败 |
| 修复版 | 0 | 8/8 通过 | `exit=0`，**57/57 全绿** |

配套 `gd_static_lint` 7/7、`check_data_refs` 通过（251 引用 / 物品 30 / 资源 8）。

### 用例 13 的设计要点（值得复用）
- **必须让玩家"死在招式判定里"**：用例 9 用的是外部 `_target.kill()`，不经过招式判定，
  所以它一直测不到这个重入 —— 补的用例要**把血量压到 1、等沙虫的招自己打过来**。
- **断言要选"功能性判据"，不要只看"日志里有没有 ERROR"**：headless 里脚本报错**不会**让退出码非 0。
  这里挂了 `attack_landed` 计数 —— 旧代码在 emit 之前就中断，计数停在 0，用例才真的会红。

### 顺带处理
- `项目结构说明.md §4.9`：状态机要点新增「扣血会同步重入」一条（含两处写法要求与两个行号出处）；
  测试表断言数 49 → 57。
- `MEMORY.md` §2 新增铁律「同步重入」；§5 Boss 条断言数同步。
- 上一轮遗留的未提交内容（记忆归档 18→2 份 + `大纲.md` 三处定稿）**单独成一个提交** `3be627f`，
  没有和代码修复混在一起；`.workbuddy/tmp/` 的三个临时脚本仍未入版本控制。

## 15:20～15:40 修「沙虫经常会变成白色看不清」（用户反馈）

### 取证（这次是**渲染出图**，不是读日志）
用户只说"变白看不清"、没截图。视觉问题不能靠猜 —— headless 渲染不出画面，
所以做了个临时截图工具：**隔离副本 + 窗口跑 `--script`**，冻结 Boss 状态机后固定机位逐状态出图，
**同时把屏幕中心（正对沙虫身体）的像素打进日志**。改前实测：

| 状态 | 取样像素 | 该状态该有的颜色 |
| --- | --- | --- |
| 常态体色 | `#ffffff` | `body_color` (0.82,0.70,0.42) |
| 前摇警示 | `#ffff90` | `telegraph_color` (1.0,0.42,0.22) |
| 濒死 / 退场 | `#ffffff` | `fallen_color` (0.52,0.50,0.55) |
| 地面 | `#ffffff` | 沙色 (0.78,0.68,0.46) |

**全是纯白**：不但沙虫和地面糊成一片，连"前摇变红 / 濒死变灰"这些**状态信号**也一起白掉。

### 根因：环境光过曝，而沙虫是工程里唯一的**受光**网格
`map.tscn` / 场地的环境光是 `ambient_light_color(0.83) × ambient_light_energy(9.0)`
⇒ 受光面整体抬亮约 **2.2 倍**，**albedo 亮过 0.45 的一律打成纯白**。
而**全工程的地形与海面本来就一律 `SHADING_MODE_UNSHADED`**（`map_generator_3d.gd`），
所以这个 9.0 一直无害 —— 直到沙虫这个用默认 `SHADING_MODE_PER_PIXEL` 的网格出现。
沙虫、场地地面、濒死灰的 albedo 全在阈值之上，于是全白；深棕的巢穴土堆（0.35,0.28,0.2）
是唯一没被打爆的（实测 #c9a06a，反证了"抬亮约 2.2 倍"这个模型）。
另：`Visual/Jaw` 连 `material_override` 都没有 ⇒ 走引擎默认的**白色受光**材质，又白一块。

### 修法（三处）
1. `boss_base._setup_placeholder_material()`：材质改 **UNSHADED**；遍历 `Visual` 下**所有**
   `MeshInstance3D` 逐个赋材质（按名字把 `Jaw` 换成暗一档的，新增 `jaw_darken`=0.32）——
   防的就是"以后加零件又漏一个"。
2. 配色：`body_color` (0.82,0.70,0.42) → **(0.58,0.45,0.26) 深沙褐**（地形瓦片色普遍偏亮，
   体色压暗一档才能在**任何**地形上看出轮廓）；`hurt_color` 近白 → **(1.0,0.66,0.52) 暖红**
   （白闪和"被打爆成白"分不清）。
3. 场地：新增 `_ensure_unshaded_materials()` 强制地面/巢穴改 UNSHADED，地面色改为
   **地图"沙地"(13) 的瓦片色**，色号 `preload` `task_system.TERRAIN_COLOR_MAP`（与地图同源）。
   ⚠ 放脚本里而不是只写 `.tscn`：本场景标签页在编辑器里开着，外部改 `.tscn` 会被覆盖（已 3 次）。

### 验证
- **新旧对跑（渲染）**：改后取样像素 —— 体色 `#947342`、前摇 `#ff6b38`、受击 `#ffa885`、
  濒死 `#857f8c`、地面 `#f2e073`，四个状态互相可分，与目标色逐一吻合。
- `gd_static_lint` 7/7；`check_data_refs` 通过（252 引用 / 物品 30 / 资源 8）；
  `boss_state_test` headless **57/57 全绿、`godot_exit=0`**（隔离副本）。

### 踩坑（可复用）
- **`_initialize()` 里一报错就永远走不到 `quit()`** ⇒ 那次的 Godot 进程挂着不退（3 分钟没动静），
  而且因为管道给了 `tail`，日志也不输出。解法：加 `--quit-after N` 兜底 + 重定向到文件。
- **辅助函数别叫 `_set`**：撞 `Object._set(StringName, Variant)` 虚函数，报 `The function signature
  doesn't match the parent`，整份脚本加载失败。
- `CanvasLayer` **不是** `Node3D`：`(child as Node3D).visible = false` 会得到 null 再赋属性 ⇒ 运行时错。
  要 `if "visible" in child: child.set("visible", false)`。
- 截图像素**比肉眼看图更硬**：一眼就能说清"白到什么程度"，也能当回归基线。

### 待办 / 提醒
- 本场景 `.tscn` 的标签页在编辑器里开着 ⇒ 那两处 `shading_mode = 0` 可能被编辑器覆盖；
  但运行时脚本会强制，功能不受影响（关掉标签页重开即可让编辑器里的预览也正确）。
- `MEMORY.md` 这轮新增两条铁律（渲染必须 UNSHADED / 视觉问题出图取证），体积约 5.9 K —— 又偏大了，
  下轮该做一次专门精简（把细节都推给 `项目结构说明.md` 与归档）。
- 截图工具是临时放在 `%TEMP%` 副本里的，**没有入库**；若以后还要反复验证美术，
  值得固化成 `test/` 下的正式工具（待用户拍板）。

## 16:00～16:10 脚本目录整理：boss 的 4 个脚本移入 `boss/sandworm/`（用户点名）

### 诉求与范围
用户：「`res://script/ai/enemy/boss/sandworm.gd` 文件夹结构不合理，应该是 `res://script/ai/enemy/boss/sandworm`，
新建 sandworm 文件夹，将沙虫相关的脚本放入其中」。
范围有歧义（`boss_state` / `boss_attack` / `boss_base` 是规格写明"三条 Boss 线共用"的骨架层），
**问清后用户选"4 个全移"** —— 即 `script/ai/enemy/boss/{ => sandworm}/` 下
`boss_state.gd` / `boss_attack.gd` / `boss_base.gd` / `sandworm.gd`（含各自 `.uid`，共 8 个文件）。
用 `git mv` ⇒ git 识别为纯 rename，历史不断。

### 同步点（移动 .gd 的完整清单，共 4 类）
1. **4 份脚本第 1 行注释头** `# script/ai/enemy/boss/xxx.gd`（易漏，但不改也不报错）。
2. **`tscn/prefab/boss/sandworm.tscn:3` 的 `ext_resource path=`** —— 该场景只写 `path=` 不写 `uid=`，
   改一处即可。（顺手确认了全工程没有别处 uid 引用这 4 个脚本。）
3. **`.godot/global_script_class_cache.cfg` 的 4 条 `path`** —— 该目录 gitignore **不入库**，
   但本地不同步的话 lint 第 4 项（类缓存一致性）会红。
4. **两份工程文档的路径表**：`项目结构说明.md` §4.9 全局类表 4 行 + `主线设计规格.md` 1.7 / 1.9 / 3.1 共 9 行。
   其中 §4.9 表格用的是**无 `script/` 前缀**的写法（`ai/enemy/boss/x.gd`），规格用的是带前缀写法。

> **sed 技巧**：用**无前缀模式** `ai/enemy/boss/X.gd` 一条规则同时命中两种写法
> （带 `script/` 前缀的路径本身就含这个子串），且不会误伤替换结果
> —— `.../boss/sandworm/boss_state.gd` 里不包含 `ai/enemy/boss/sandworm.gd` 这个子串。
> 顺序：先替换具体名（`boss_state` 等）即可，`sandworm.gd` 放最后也安全。

### 不必改的地方
类名调用全走 `class_name`（`BossBase` / `BossState` / `BossAttack` / `Sandworm` / `WorldState`）
⇒ **测试脚本、场地脚本、`.tscn` 里的节点脚本挂载全都不用动**。这条是"用 `class_name` 而非 `preload 路径`"的回报。

### 验证
| 手段 | 结果 |
| --- | --- |
| `gd_static_lint` | **7/7**（第 4 项类缓存一致性 OK ⇒ 手改的 4 条缓存路径正确） |
| `check_data_refs` | **252 条 `res://` 引用全部有效** ⇒ `.tscn → 新脚本路径`正确 |
| `boss_state_test`（隔离副本 headless） | **57/57 全绿** |
| Godot **自己扫描**生成的类缓存（副本全新导入后） | 4 条路径全指向 `boss/sandworm/` ⇒ **编辑器重扫会自己写对**，不依赖我手改 |

### 踩坑（重要，可复用）
- **`--headless --script` 不扫描项目，不会生成 `.godot`。** 副本删掉 `.godot` 后直接跑测试 ⇒
  **所有** `class_name` 都报 `not declared in the current scope`（连完全没动过的 `WorldState` 也报），
  极像"移动把类搞坏了"，其实是环境缺类缓存。
  **必须先让副本生成 `.godot`**：`godot --headless --path <副本> --editor --quit`。
  （单独 `--import` 不可靠、当时没生成；`--editor --quit` 实测一步到位。这步顺带等于模拟了编辑器首扫。）
- **Steam 版 Godot 是 Windows GUI 子系统**：PowerShell 里 `& $godot ...` **不等待进程**、
  `$LASTEXITCODE` 为空、stdout **不进管道**（所以别指望 `Select-Object -Last N` 拿到结果）。
  ⇒ 结果去 `%APPDATA%\Godot\app_userdata\loss_land\logs\godot.log` 读，且要**轮询等它跑完**
  （`godot.log` 恒为"当次"，上一次会被归档成 `godot<ISO时间戳>.log`）。
  ⚠ 轮询关键字别用「全部」——「全部招式在 CD 时…」会立刻误命中；要用 `N/N 项` 这类形态。
- 副本目录清理时被 Godot 文件锁占用，**第一次 `Remove-Item -Recurse` 会失败**，隔几秒重试即可。
  真因不是"锁延迟"：**那次 headless 测试进程跑完后没有自行退出**（PID 仍在，已打印完"57/57"）。
  得先 `Stop-Process` 掉残留进程，副本才删得掉。⇒ 跑完 headless 后顺手 `Get-Process | ? ProcessName -like "*godot*"`
  核对一下，别让残留进程下次抢 `.godot`。

### 遗留
- 结构上 `boss/` 现在只剩 `sandworm/` 一层。将来加第二条 Boss 线（规格里的 `giant_bird` / `sword_guardian`）时，
  「骨架三件套」是否上提到 `boss/` 或 `boss/common/` 需要重新拍板 —— 已作为**建议**列给用户，未擅自调整。
- `tscn/prefab/boss/` 仍是独立的 `boss` 目录（本次只整理了 `script/` 侧），是否一并收进 `sandworm/` 同样待拍板。

## 16:35～17:05 沙虫脚本加 `sandworm` 前缀（文件名 + 类名）+ 场地唤起键 W→G（用户点名）

### 诉求
1. 「唤起不要用 w，此按键冲突了」—— 查明冲突源：`project.godot` 里 `move_forward` 绑的就是**物理键 W(87)**，
   所以按 W 唤起时玩家同时前进。已占键：W/S/A/D、空格、Q/E、F、C、B、M、Tab、1–9；场地自用 T/H/K/R。
   **问清后改绑 G**（自由键、与 T/H/K/R 同片左手区）。
2. 「`boss_state.gd` / `boss_attack.gd` / `boss_base.gd` 改成 `boss_sandworm_*` 更合理」——
   追问「类名是否一起改」（因为规格写明这三件套是"三条 Boss 线共用"的骨架层），用户选**文件名 + 类名一起改**。

### 改动面
| 项 | 数量 |
| --- | --- |
| 文件重命名（3 × `.gd` + `.uid`，`git mv`） | 6 |
| 类名替换 `BossState/BossAttack/BossBase` → `BossSandworm*` | **163 处**（8 个文件） |
| 旧文件名引用 `boss_*.gd` → `boss_sandworm_*.gd` | 16 处 |
| 唤起键 W→G | 6 处（3 个文件，含两份文档的场地按键说明） |
| 类缓存 `class` + `path` | 4 条（`Sandworm` 的 `base` 也跟着改了） |

**sed 一次搞定的关键**：用 `\bBossState\b` 词边界替换类名；文件名用无前缀模式，
`.../sandworm/boss_state.gd` 替换后是 `boss_sandworm_state.gd`，不会与 `sandworm.gd` 互相误伤。
**`sandworm.gd` 本身没改名 ⇒ `tscn/prefab/boss/sandworm.tscn` 的 ext_resource 不用动。**

### 验证
| 手段 | 结果 |
| --- | --- |
| `gd_static_lint` | **7/7** |
| `check_data_refs` | **252 条 `res://` 引用全有效** |
| Godot 自扫类缓存（副本全新导入） | `BossSandwormState/Attack/Base` + `Sandworm`(base=`BossSandwormBase`)，旧类名 **0 残留** |
| `boss_state_test` headless | **57/57 全绿、exit=0，且零 ERROR / 零 WARNING** |
| 场地实跑 | 面板与 `_note` 已显示「G 唤起」 |

### 踩坑（都是"环境/工具"，不是代码）
- **Bash 里 `export APPDATA` 必须用 Windows 格式路径**（`C:/Users/...`）。写成 `/c/Users/...` 会让 Godot 报
  `Could not create directory: '/c'`、`user://` 建不出来、**日志根本不落盘**。
  ⚠ ※注意：旧记忆只写了「Git Bash 跑先补 `APPDATA`」，**没说格式** —— 这次就栽在这。
- **PowerShell 跑 GUI 子系统的 Godot 不可信**：`& $godot ...` 不等待 ⇒ 命令"完成"但进程还没跑完、
  stdout 被截断、后续语句可能整段不执行（本次 robocopy 都没跑成）。**Bash 会正常等待子进程** ⇒
  验证一律 `bash: "$GODOT" ... > out.txt 2>&1; echo exit=$?`，一次拿到完整输出与退出码。
- **tar 复制副本要连工程内的 `Godot/` 目录一起排除**（那是 `user://` 日志目录）。
  这次只排除了 `.godot`，结果读到副本带来的**上一轮别的测试的旧 godot.log**（storage_transfer_test 的 33 项），
  差点误判成"本次结果"。**读测试结果前先确认日志的时间戳/内容对得上。**
- **`--editor --quit` 跑一次不会导入全部美术**：`char_blue.png` 缺 `.ctex` ⇒ `player.tscn` 解析失败 ⇒
  连坐 `sandworm_test.tscn:68` 报 `referenced non-existent resource`。**这跟代码改动无关**
  （主工程该资源有 9 个导入产物）。要"完整环境"，直接把主工程 `.godot` 整个 `cp -r` 过去，
  比等二次扫描快 —— 换成完整 `.godot` 后同一条测试**零 ERROR 零 WARNING**。

### 遗留（已列给用户，等拍板，我未擅自改）
类名带上沙虫后，有 5 处"通用骨架"叙述变得名实不符：
1. `boss_sandworm_base.gd:3` `# Boss 通用状态机骨架（三条 Boss 线共用）`
2. `boss_sandworm_base.gd:9` 「三条 Boss 线照抄就是…」
3. `boss_sandworm_state.gd:8` 「三条 Boss 线（沙虫地表 / 机械沙虫 / 巨鸟）共用同一套状态语义」
4. `主线设计规格.md:32` 「新起一层 `BossSandwormBase`」（原意是给三条线新起一层）
5. `主线设计规格.md:221` 表格备注「所有 Boss 的共同父类」

